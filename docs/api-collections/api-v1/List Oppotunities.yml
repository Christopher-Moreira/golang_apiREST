info:
  name: List Oppotunities
  type: http
  seq: 5

http:
  method: GET
  url: http://127.0.0.1:8080/api/v1/openings
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
